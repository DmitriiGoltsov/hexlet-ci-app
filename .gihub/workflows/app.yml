name: hexlet CI app

on:
  - push
  - pull_request

jobs:

  build:
    
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    
    steps:
      
      - run: make setup
      - run: make start
      - run: make test
      - run: make lint
