name: hexlet CI app

on:
  - push
  - pull_request

jobs:

  build:
    
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    
    steps:
      
      - run: make install
      - run: make build
      - run: make test
      - run: make lint
